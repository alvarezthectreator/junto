# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Database (SQLite for development - auto-used if DATABASE_URL is not set)
MOCK_DATA=true

# PostgreSQL (Production on Railway - uncomment and update)
# DATABASE_URL=postgresql://username:password@localhost:5432/junto
# For Railway, the DATABASE_URL is automatically set by Railway
MOCK_DATA=true
