VITE_API_BASE_URL=/api
# In production, keep this as /api so Vercel can proxy requests to the backend host.
# If you use a real backend origin, set it here only if it supports HTTPS and no mixed-content issues.
